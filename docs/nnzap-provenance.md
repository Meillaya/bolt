# nnzap parity provenance note — Milestone 0

## Authorization
The user explicitly authorized direct porting/adaptation from the local `reference/nnzap` tree for this project. This note records that instruction for implementation planning.

## Boundaries
- The authorization applies to local development in this repository.
- Before redistribution, publication, or external production use, confirm ownership/license status for `reference/nnzap` and all model/data assets.
- Do not commit large third-party model/data assets unless explicitly approved.
- For code adapted from `reference/nnzap`, commits should mention the adapted reference module and preserve any required notices if later license evidence requires them.
- For behavior reimplemented without direct copying, cite the observed reference command/API/test behavior in the parity inventory or implementation notes.

## Asset provenance
Final-gate assets require a completed manifest with local path, source, provenance, license/access note, checksums, paired tokenizer/model files, and acceptance use.

## Escalation
If provenance becomes ambiguous, stop the affected implementation lane and either document clean-room behavior-only reimplementation or obtain explicit rights evidence before continuing.
