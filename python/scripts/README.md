# Python scripts

Planned scripts:

- `generate_goldens.py`
- `compare_outputs.py`

These now exist as deterministic scaffolding so the fixture format can stabilize before the first real model outputs land.
